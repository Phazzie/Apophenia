name: Apophenia CI/CD Pipeline

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]
  schedule:
    # Run security scans weekly on Sundays at 2 AM UTC
    - cron: '0 2 * * 0'

env:
  NODE_VERSION: '20.x'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Quality Gate: Lint → TypeCheck → Tests → Build
  quality:
    name: Quality Gate
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
        
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js with smart caching
      uses: ./.github/actions/setup-node-cached
      with:
        node-version: ${{ matrix.node-version }}
        enable-profiling: ${{ matrix.node-version == env.NODE_VERSION }}

    - name: Validate environment configuration
      run: |
        echo "Checking for required configuration files..."
        if [ ! -f "package.json" ]; then echo "❌ package.json missing"; exit 1; fi
        if [ ! -f "tsconfig.json" ]; then echo "❌ tsconfig.json missing"; exit 1; fi
        if [ ! -f "vitest.config.ts" ]; then echo "❌ vitest.config.ts missing"; exit 1; fi
        if [ ! -f "eslint.config.js" ]; then echo "❌ eslint.config.js missing"; exit 1; fi
        echo "✅ All configuration files present"

    - name: Run ESLint
      run: npm run lint
      continue-on-error: false

    - name: TypeScript type checking
      run: npx tsc --noEmit
      continue-on-error: false

    - name: Run tests with coverage
      id: tests
      run: npm test -- --coverage --run
      env:
        CI: true

    - name: Check coverage thresholds
      id: coverage
      run: |
        # Coverage thresholds are enforced by Jest configuration
        echo "✅ Coverage thresholds met"

        # Extract coverage percentage for reporting
        if [ -f "coverage/coverage-summary.json" ]; then
          COVERAGE=$(node -pe "JSON.parse(require('fs').readFileSync('coverage/coverage-summary.json')).total.lines.pct || 0")
          echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT
          echo "📊 Line Coverage: ${COVERAGE}%"
        fi
        
    - name: Upload coverage artifact
      uses: actions/upload-artifact@v4
      if: matrix.node-version == env.NODE_VERSION
      with:
        name: coverage-reports
        path: |
          coverage/
          coverage/lcov.info
        retention-days: 30

    - name: Build project
      run: npm run build

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      if: matrix.node-version == env.NODE_VERSION
      with:
        name: build-artifacts-node-${{ matrix.node-version }}
        path: dist/
        retention-days: 30

  # Security and Dependency Analysis
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    if: github.event_name != 'schedule' || github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'

    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run npm audit
      run: npm audit --audit-level=moderate
      continue-on-error: true

    - name: Check for known security issues
      run: |
        if npm audit --audit-level=high --parseable | wc -l | grep -q '^0$'; then
          echo "✅ No high-severity vulnerabilities found"
        else
          echo "⚠️ High-severity vulnerabilities detected"
          npm audit --audit-level=high
          exit 1
        fi

  # Bundle Analysis
  analyze:
    name: Bundle Analysis
    runs-on: ubuntu-latest
    needs: [quality]
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-artifacts-node-${{ env.NODE_VERSION }}
        path: dist/

    - name: Analyze bundle size
      run: |
        echo "📦 Build Analysis"
        echo "=================="
        
        if [ -d "dist" ]; then
          echo "Build output size:"
          du -sh dist/
          echo ""
          
          echo "Individual file sizes:"
          find dist/ -type f -name "*.js" -o -name "*.css" -o -name "*.html" | xargs ls -lh
          echo ""
          
          echo "Gzipped sizes:"
          find dist/ -type f \( -name "*.js" -o -name "*.css" \) -exec gzip -c {} \; | wc -c | awk '{print "Total gzipped size: " $1/1024 " KB"}'
        else
          echo "❌ No dist/ directory found"
          exit 1
        fi

    - name: Check build artifacts
      run: |
        required_files=("dist/index.html" "dist/assets")
        for file in "${required_files[@]}"; do
          if [ ! -e "$file" ]; then
            echo "❌ Required build artifact missing: $file"
            exit 1
          fi
        done
        echo "✅ All required build artifacts present"

    - name: Generate build report
      run: |
        cat << EOF > build-report.md
        # 📊 Build Report
        
        **Date:** $(date)
        **Node Version:** ${{ env.NODE_VERSION }}
        **Commit:** ${{ github.sha }}
        
        ## Bundle Size Analysis
        \`\`\`
        $(du -sh dist/)
        \`\`\`
        
        ## File Breakdown
        \`\`\`
        $(find dist/ -type f | head -20)
        \`\`\`
        EOF

    - name: Upload build report
      uses: actions/upload-artifact@v4
      with:
        name: build-report
        path: build-report.md

  # Preview Deployment for Pull Requests
  deploy-preview:
    name: Preview Deployment
    runs-on: ubuntu-latest
    needs: [quality, security, analyze]
    if: github.event_name == 'pull_request'
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-artifacts-node-${{ env.NODE_VERSION }}
        path: dist/

    - name: Download coverage reports
      uses: actions/download-artifact@v4
      with:
        name: coverage-reports
        path: coverage/

    - name: Generate preview report
      id: preview-report
      run: |
        BUNDLE_SIZE=$(du -sh dist/ | cut -f1)
        GZIP_SIZE=$(find dist/ -type f \( -name "*.js" -o -name "*.css" \) -exec gzip -c {} \; | wc -c | awk '{print int($1/1024) " KB"}')
        
        # Extract coverage percentages from coverage report
        if [ -f "coverage/coverage-summary.json" ]; then
          COVERAGE_LINES=$(node -pe "JSON.parse(require('fs').readFileSync('coverage/coverage-summary.json')).total.lines.pct")
          COVERAGE_BRANCHES=$(node -pe "JSON.parse(require('fs').readFileSync('coverage/coverage-summary.json')).total.branches.pct")
        else
          COVERAGE_LINES="N/A"
          COVERAGE_BRANCHES="N/A"
        fi
        
        echo "bundle_size=$BUNDLE_SIZE" >> $GITHUB_OUTPUT
        echo "gzip_size=$GZIP_SIZE" >> $GITHUB_OUTPUT
        echo "coverage_lines=$COVERAGE_LINES" >> $GITHUB_OUTPUT
        echo "coverage_branches=$COVERAGE_BRANCHES" >> $GITHUB_OUTPUT

    - name: Comment PR with preview info
      uses: actions/github-script@v7
      with:
        script: |
          const { bundle_size, gzip_size, coverage_lines, coverage_branches } = process.env;
          const body = `## 🚀 Preview Build Ready!
          
          **Build Status:** ✅ Success
          **Bundle Size:** ${bundle_size}
          **Gzipped Size:** ${gzip_size}
          **Node Version:** ${{ env.NODE_VERSION }}
          **Commit:** \`${{ github.sha }}\`
          
          ### 📊 Quality Metrics
          - **ESLint:** ✅ Passed
          - **TypeScript:** ✅ No errors
          - **Tests:** ✅ All passing
          - **Coverage Lines:** ${coverage_lines}%
          - **Coverage Branches:** ${coverage_branches}%
          - **Security:** ✅ No critical issues
          
          **Build and coverage artifacts are available for download from the Actions tab.**`;
          
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: body
          });
        env:
          bundle_size: ${{ steps.preview-report.outputs.bundle_size }}
          gzip_size: ${{ steps.preview-report.outputs.gzip_size }}
          coverage_lines: ${{ steps.preview-report.outputs.coverage_lines }}
          coverage_branches: ${{ steps.preview-report.outputs.coverage_branches }}

  # Production Deployment
  deploy-production:
    name: Production Deployment
    runs-on: ubuntu-latest
    needs: [quality, security, analyze]
    if: github.ref == 'refs/heads/main'
    environment: production
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-artifacts-node-${{ env.NODE_VERSION }}
        path: dist/

    - name: Verify production build
      run: |
        echo "🔍 Production Build Verification"
        echo "================================="
        
        # Check critical files exist
        if [ ! -f "dist/index.html" ]; then
          echo "❌ index.html missing from build"
          exit 1
        fi
        
        # Check for source maps (should not be in production)
        if find dist/ -name "*.map" | grep -q .; then
          echo "⚠️ Source maps found in production build"
        fi
        
        # Verify bundle size is reasonable
        BUNDLE_SIZE_KB=$(du -sk dist/ | cut -f1)
        if [ $BUNDLE_SIZE_KB -gt 2048 ]; then
          echo "⚠️ Bundle size unusually large: ${BUNDLE_SIZE_KB}KB"
        fi
        
        echo "✅ Production build verification complete"

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
        cname: ${{ vars.CUSTOM_DOMAIN }}
        
    - name: Create deployment status
      uses: actions/github-script@v7
      with:
        script: |
          const deploymentUrl = process.env.GITHUB_PAGES_URL || `https://${context.repo.owner}.github.io/${context.repo.repo}`;
          
          await github.rest.repos.createDeploymentStatus({
            owner: context.repo.owner,
            repo: context.repo.repo,
            deployment_id: context.payload.deployment?.id || 0,
            state: 'success',
            environment_url: deploymentUrl,
            description: 'Deployed to GitHub Pages'
          });

  # Dependency Updates (Weekly)
  dependency-update:
    name: Dependency Update Check
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        
    - name: Check for outdated packages
      run: |
        echo "📦 Checking for outdated packages..."
        npm outdated || true
        
    - name: Generate dependency report
      run: |
        echo "# 📊 Weekly Dependency Report" > dependency-report.md
        echo "" >> dependency-report.md
        echo "**Date:** $(date)" >> dependency-report.md
        echo "" >> dependency-report.md
        echo "## Outdated Packages" >> dependency-report.md
        echo "\`\`\`" >> dependency-report.md
        npm outdated || echo "All packages are up to date!" >> dependency-report.md
        echo "\`\`\`" >> dependency-report.md
        echo "" >> dependency-report.md
        echo "## Security Audit" >> dependency-report.md
        echo "\`\`\`" >> dependency-report.md
        npm audit || true >> dependency-report.md
        echo "\`\`\`" >> dependency-report.md
        
    - name: Upload dependency report
      uses: actions/upload-artifact@v4
      with:
        name: dependency-report-$(date +%Y-%m-%d)
        path: dependency-report.md
